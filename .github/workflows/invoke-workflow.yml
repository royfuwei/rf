name: Invoke Reusable Workflow

on:
  push:
    branches:
      - none

jobs:
  call-reusable:
    uses: royfuwei/rf-devops/.github/workflows/_reusable-workflow.yml@main
    with:
      example_input: "World"
